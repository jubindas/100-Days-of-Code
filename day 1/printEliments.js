// Print all elements of an array

let fruits = ["apple", "banana", "cherry", "date", "elderberry"];

fruits.forEach((item, index) => {
  console.log("the fruits are no " + index + " and the name is " + item);
});
